[project]
name = "share-with-us"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "django==5.2.6",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
